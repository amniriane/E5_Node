

extends layout.pug

block content
  h1 Hello Mongo!

  p Explorer la base de restaurants de la ville de New York avec cette application !

  form(method="get") 
    input(type="text")
  ul
    each name in names
      li= name